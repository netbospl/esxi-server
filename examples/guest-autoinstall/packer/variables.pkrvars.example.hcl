# TEMPLATE ONLY.
# Keep sensitive values in a local secret store or a non-committed .pkrvars.hcl file.

vcenter_server = "example.local"
username       = "REPLACE_WITH_USERNAME"
password       = "REPLACE_WITH_PASSWORD"
datacenter     = "REPLACE_WITH_DATACENTER"
cluster        = "REPLACE_WITH_CLUSTER"
host           = "REPLACE_WITH_HOST"
datastore      = "REPLACE_WITH_DATASTORE"
network        = "REPLACE_WITH_NETWORK"
iso_path       = "REPLACE_WITH_ISO_PATH"
iso_checksum   = "sha256:REPLACE_WITH_ISO_CHECKSUM"
vm_name        = "REPLACE_WITH_VM_NAME"
